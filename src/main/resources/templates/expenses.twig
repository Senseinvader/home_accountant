<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Expenses</title>
    <link rel="stylesheet" href="/static/css/styles.css">
    <link href="https://fonts.googleapis.com/css?family=Lato" rel="stylesheet">
</head>
<body>

<div class="page-name-container">
    <p>Choose dates of report:</p>
</div>
<div class="form-container">
    <form action="#" method="post">
        <div class="input-container">
            <label for="date-from">Date from*</label>
            <input type="date" class="input" id="date-from" name="date-from" required>
        </div>
        <div class="input-container">
            <label for="date-to">Date to*</label>
            <input type="date" class="input" id="date-to" name="date-to" required>
        </div>
        <div class="button-container input-container">
            <input type="submit" class="button" name="submit" value="Show results">
        </div>
    </form>
</div>

<div class="page-name-container">
    <p>Expenses of {{ name }} from {{ dateOne }} to {{ dateTwo }}</p>
</div>

<div class="results-container">
    <table>
        <thead>
        <tr>
            <th>Date</th>
            <th>Amount</th>
            <th>Category</th>
            <th>Comment</th>
        </tr>
        </thead>
        <tbody>
        {% for expense in expenses %}
        <tr>
            <td>{{ expense.getStringPurchaseDate() }}</td>
            <td>{{ expense.getExpenseAmount() }}</td>
            <td>{{ expense.getExpenseCategoryName() }}</td>
            <td>{{ expense.getComment() }}</td>
        </tr>
        {% endfor %}}
        </tbody>
    </table>
</div>

</body>
</html>